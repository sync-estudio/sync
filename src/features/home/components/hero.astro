---
import "@app/styles/global.css";
import Button from "@shared/components/ui/button.astro";
---

<div
    id="inicio"
    data-hero-card
    class="hero-card relative isolate container mx-auto min-h-screen flex justify-center items-center pt-20 pb-12 md:pb-16 px-6 sm:px-10 md:px-14 lg:px-20"
>
    <pre data-hero-wave class="hero-wave" aria-hidden="true"></pre>
    <div
        class="flex flex-col gap-6 md:gap-7 w-full max-w-5xl relative z-10"
    >
        <div class="text-zinc-800 text-center">
            <h1
                class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold mb-4 md:mb-5 leading-[1.05] tracking-tight text-balance"
            >
                <span data-hero-line class="hero-line"
                    >Construimos el <span
                        class="text-emerald-700"
                        >futuro</span
                    > de tu operación.</span
                >
            </h1>
            <p
                data-hero-line
                class="hero-line text-sm sm:text-base md:text-lg text-zinc-500 max-w-2xl mx-auto leading-relaxed text-balance"
            >
                IA y herramientas a medida para empresas que ya operan. Quitamos el trabajo manual, desatascamos la operación y dejamos a tu equipo dueño del sistema.
            </p>
        </div>

        <div
            data-hero-actions
            class="flex flex-wrap justify-center gap-3 sm:gap-3 md:gap-4 w-full px-4 sm:px-0 hero-actions"
        >
            <Button link="/contacto" style="w-full sm:w-auto shadow-sm">Solicitar diagnóstico</Button>
            <Button link="/#metodologia" variant="outline" style="w-full sm:w-auto !bg-white/70 backdrop-blur-md !border-zinc-300/80 shadow-sm hover:!bg-white/90">Ver cómo trabajamos</Button>
        </div>
    </div>
</div>

<style>
    .hero-card {
        position: relative;
    }

    .hero-wave {
        position: absolute;
        inset: 0;
        margin: 0;
        pointer-events: none;
        z-index: 0;
        overflow: hidden;
        font-family: ui-monospace, "SF Mono", Menlo, Consolas, monospace;
        font-weight: 700;
        color: rgba(4, 120, 87, 0.45);
        white-space: pre;
        user-select: none;
        line-height: 1;
        letter-spacing: 0.4em;
        mask-image: linear-gradient(
            90deg,
            transparent 0%,
            #000 3%,
            #000 97%,
            transparent 100%
        );
        -webkit-mask-image: linear-gradient(
            90deg,
            transparent 0%,
            #000 3%,
            #000 97%,
            transparent 100%
        );
    }

    .hero-line {
        display: block;
    }

    .hero-actions {
        animation: hero-actions-in 0.76s cubic-bezier(0.22, 1, 0.36, 1) 0.35s both;
    }

    @keyframes hero-actions-in {
        from { opacity: 0; transform: translateY(24px); }
        to   { opacity: 1; transform: translateY(0); }
    }

    @media (prefers-reduced-motion: reduce) {
        .hero-actions {
            animation: none !important;
            opacity: 1 !important;
            transform: none !important;
        }
    }

    @media (min-width: 640px) {
        #inicio img {
            max-height: 600px;
            min-height: 400px;
        }
    }

    @media (min-width: 768px) {
        #inicio img {
            max-height: 700px;
            min-height: 500px;
        }
    }

    @media (min-width: 1024px) {
        #inicio img {
            max-height: 800px;
            min-height: 600px;
        }
    }

    @media (min-width: 1280px) {
        #inicio img {
            max-height: 900px;
            min-height: 700px;
        }
    }
</style>

<script>
    const RAMP = "  ···";
    const FONT_PX = 13;
    const CELL_W = FONT_PX;
    const CELL_H = FONT_PX;

    function initWave(el: HTMLElement) {
        const card = el.parentElement as HTMLElement;
        const reduce = window.matchMedia(
            "(prefers-reduced-motion: reduce)",
        ).matches;

        let cols = 0;
        let rows = 0;
        let frame = 0;
        let base = 0;
        let ampA = 0;
        let ampB = 0;
        let thickness = 1;
        let freqA = 7;
        let freqB = 13;

        el.style.fontSize = FONT_PX + "px";

        function resize() {
            const rect = card.getBoundingClientRect();
            cols = Math.max(1, Math.floor(rect.width / CELL_W));
            rows = Math.max(1, Math.floor(rect.height / CELL_H));

            // Keep wavelength ~constant in px: ~one crest per 640px.
            const cycles = Math.max(1, rect.width / 640);
            freqA = cycles * Math.PI * 2;
            freqB = freqA * 1.85;

            // Push wave below the text on all screens.
            base = rows * 0.72;
            // Cap amplitude by width so narrow/tall mobile cards stay gentle.
            ampA = Math.min(rows * 0.26, cols * 0.14);
            ampB = ampA * 0.5;
            thickness = Math.max(2, rows * 0.14);
        }

        function render(t: number) {
            const out: string[] = [];

            for (let y = 0; y < rows; y++) {
                let line = "";
                for (let x = 0; x < cols; x++) {
                    const u = x / cols;
                    const center =
                        base +
                        ampA * Math.sin(u * freqA + t) +
                        ampB * Math.sin(u * freqB - t * 0.7);
                    const d = Math.abs(y - center) / thickness;
                    // smooth bell falloff for gentle edges, constant along x
                    const intensity = Math.exp(-d * d * 1.4);
                    if (intensity <= 0.06) {
                        line += " ";
                    } else {
                        const idx = Math.min(
                            RAMP.length - 1,
                            Math.round(intensity * (RAMP.length - 1)),
                        );
                        line += RAMP[idx];
                    }
                }
                out.push(line);
            }
            el.textContent = out.join("\n");
        }

        function loop() {
            frame += 0.005;
            render(frame);
            requestAnimationFrame(loop);
        }

        let lastW = -1;
        function onResize() {
            const w = Math.round(card.getBoundingClientRect().width);
            // Ignore height-only changes (mobile address-bar show/hide on scroll).
            if (w === lastW) return;
            lastW = w;
            resize();
            if (reduce) render(0);
        }

        onResize();
        window.addEventListener("resize", onResize);
        window.addEventListener("orientationchange", onResize);

        if (reduce) {
            render(0);
        } else {
            requestAnimationFrame(loop);
        }
    }

    document
        .querySelectorAll<HTMLElement>("[data-hero-wave]")
        .forEach(initWave);
</script>

